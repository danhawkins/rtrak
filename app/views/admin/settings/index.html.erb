<h3>Settings</h3>
<% form_for :setting, :url => admin_settings_url, :method => :put do |f| %>
		<%= f.label :site_name %>
		<%= f.text_field :site_name ,:value => @settings_hash['site_name'] %>

		<%= f.submit 'Save Settings' %>
<% end %>
