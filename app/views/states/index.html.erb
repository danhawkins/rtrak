<% title "States" %>

<table>
  <tr>
    <th>Name</th>
    <th>Openstate</th>
  </tr>
  <% for state in @states %>
    <tr>
      <td><%=h state.name %></td>
      <td><%=h state.openstate %></td>
      <td><%= link_to "Show", state %></td>
      <td><%= link_to "Edit", edit_state_path(state) %></td>
      <td><%= link_to "Destroy", state, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New State", new_state_path %></p>
